# mini-shop-server 纵向测试分工方案

> 文档版本：v1.0
> 编写日期：2026-06-23
> 适用范围：期末大作业报告剩余测试任务分配
> 前置条件：黑盒测试 + 接口测试已完成（不纳入本次分工）

---

## 1. 分工背景

### 当前问题

之前的测试按**模块横向分工**（1号测auth/user/product/order，2号测group/menu/config/dict……），每个人都在做**同一层次**的接口测试，导致：

- 报告第2章六个测试类型中，只有"接口测试"有实质内容
- 黑盒、白盒、单元、集成、系统测试内容缺失
- 每个人只锻炼了一种测试能力

### 改进方向

改为**纵向分工**：按测试层次/类型划分，每人负责一个测试维度，覆盖全项目模块。

### 评分权重对照

| 测试类型 | 报告章节 | 分值 | 状态 |
|---------|---------|------|------|
| 黑盒测试 | §2.1 | 5分 | ✅ 已完成 |
| 白盒测试 | §2.2 | 5分 | ❌ 待分配 |
| 单元测试 | §2.3 | 10分 | ❌ 待分配 |
| 集成测试 | §2.4 | 15分 | ❌ 待分配 |
| 接口测试 | §2.5 | 25分 | ✅ 已完成 |
| 系统测试 | §2.6 | 30分 | ❌ 待分配 |
| 理论知识 | — | 10分 | — |

---

## 2. 最终分工总表

| 成员 | 测试类型 | 报告章节 | 分值 | 定位 |
|------|---------|---------|------|------|
| **1号** | 单元测试 + 白盒测试 | §2.2 + §2.3 | 15分 | 深入代码内部，测函数级正确性 |
| **2号** | 集成测试 | §2.4 | 15分 | 串联多个组件，测模块间协作 |
| **3号** | 系统测试A：性能 + 安全 | §2.6（前） | ~15分 | 压测并发、渗透安全漏洞 |
| **4号** | 系统测试B：恢复 + 部署 + 稳定性 | §2.6（后） | ~15分 | 异常恢复、环境切换、长期运行 |

### 为什么这样分配

- **白盒+单元合并**：单元测试天然需要用白盒方法（看代码内部分支才能写测试），是同一项工作的两个产出角度，不可拆分
- **系统测试拆分**：30分是单元/集成的两倍，一人完成工作量过重，拆成"性能+安全"和"恢复+部署+稳定性"两块，方法论不同、内容不重复
- **3号工作量较轻**（接口测试已完成），系统测试的"性能+安全"更偏攻击性测试，和之前接口测试的经验衔接最好

---

## 3. 1号 — 单元测试 + 白盒测试

### 测试范围

只测 `app/core/` 公共层，这是所有模块的代码基石，最有代表性。

### 单元测试清单（10个函数，~12个用例）

| 编号 | 源文件 | 被测函数 | 用例要点 |
|------|--------|---------|---------|
| U-01 | `token_auth.py` | `generate_auth_token` | 正常生成 / 不同scope / expiration参数验证 |
| U-02 | `token_auth.py` | `decrypt_token` | 有效token解析 / 空token / BadSignature / SignatureExpired |
| U-03 | `validator.py` | `isPositiveInteger` | 正整数 / 0 / 负数 / 字符串 / None |
| U-04 | `validator.py` | `isNaturalNumber` | 自然数 / 负数 / 非数字类型 |
| U-05 | `validator.py` | `isList` / `isEmptyList` | 列表 / 空列表 / 非列表 |
| U-06 | `utils.py` | `as_namedtuple` | 普通字典 / 空字典 / 含None跳过 |
| U-07 | `auth.py` | `get_ep_name` | 存在的ID返回名称 / 不存在的ID抛NotFound |
| U-08 | `auth.py` | `find_auth_module` | 存在的模块返回meta / 不存在返回None |
| U-09 | `auth.py` | `is_in_auth_scope` | meta存在且匹配 / meta存在不匹配 / meta不存在 |
| U-10 | `utils.py` | `get_request_args` | body+query合并 / 仅body / 仅query |

### 白盒测试清单（3张流程图 + 分支覆盖表）

| 编号 | 函数 | 分支数 | 覆盖路径 |
|------|------|--------|---------|
| W-01 | `decrypt_token` | 4 | ①正常解析 ②BadSignature ③SignatureExpired ④空值异常 |
| W-02 | `is_in_auth_scope` | 3 | ①meta存在且权限匹配→True ②meta存在权限不匹配→False ③meta不存在→False |
| W-03 | `isPositiveInteger` + `isNaturalNumber` | 组合 | 绘制判定表，覆盖所有输入类型组合 |

### 产出目录

```
tests/unit/
├── __init__.py
├── test_validator.py       # U-03, U-04, U-05
├── test_token_auth.py      # U-01, U-02
├── test_utils.py           # U-06, U-10
└── test_auth.py            # U-07, U-08, U-09
```

### 报告产出

- §2.2 白盒测试：3张流程图 + 分支覆盖表 + 测试执行结果
- §2.3 单元测试：10个函数测试用例汇总 + 执行通过率

---

## 4. 2号 — 集成测试

### 测试范围

选6条跨组件端到端链路，覆盖全项目核心交互。

### 集成测试清单（6条链路）

| 编号 | 链路名称 | 涉及组件 | 验证点 | 来源模块 | 难度 |
|------|---------|---------|--------|---------|------|
| I-01 | 登录→Token验证→访问受限接口 | API → `verify_password` → `decrypt_token` → `User.get_or_404` → `g.user` | Token生成正确、鉴权中间件执行、用户信息注入 | 1号 | ⭐ |
| I-02 | 注册→登录→修改密码→新密码登录 | API → `UserDao.create_user` → Identity → `change_password` → Token刷新 | 注册后可登录、改密码后旧密码失效、新密码可用 | 1号 | ⭐⭐ |
| I-03 | 下单→库存扣减→快照→支付回调 | API → `OrderDao` → `ProductDao` → DB事务(`auto_commit`) → 回调处理 | 订单入库、库存原子扣减、快照数据完整、回调后状态更新 | 1号+3号 | ⭐⭐⭐ |
| I-04 | 管理员鉴权→RBAC权限组校验 | API → `verify_group` → `is_in_auth_scope` → `Auth.get` | 管理员访问CMS成功、普通用户被拒绝 | 2号 | ⭐⭐ |
| I-05 | 文件上传→存储→静态资源访问 | API → File存储 → Static路由 | 上传成功返回路径、静态URL可访问 | 4号 | ⭐⭐ |
| I-06 | 身份绑定→解绑→重复绑定拦截 | API → `IdentityDao` → DB唯一性校验 | 绑定成功、解绑后可重新绑定、重复绑定被拒绝 | 1号 | ⭐⭐ |

### 产出目录

```
tests/integration/
├── __init__.py
├── conftest.py                       # 集成测试公共fixture
├── test_auth_flow.py                 # I-01
├── test_user_lifecycle.py            # I-02, I-06
├── test_order_flow.py                # I-03
├── test_rbac_flow.py                 # I-04
└── test_file_flow.py                 # I-05
```

### 报告产出

- §2.4 集成测试：6条链路测试设计（含时序图）+ 执行结果 + 数据库验证截图

---

## 5. 3号 — 系统测试A（性能 + 安全）

### 测试范围

性能压测3个场景 + 安全渗透5个场景，共8个场景。

### 系统测试清单（8个场景）

| 编号 | 类型 | 场景描述 | 测试方法 | 目标模块 |
|------|------|---------|---------|---------|
| S-01 | 性能 | 登录接口并发压测 | 50/100/200并发调用 `/v1/token`，记录P50/P95/P99响应时间、TPS | 1号 |
| S-02 | 性能 | 商品列表并发查询 | 200并发调用 `/v1/products`，记录错误率、吞吐量 | 1号 |
| S-03 | 性能 | 下单并发（防超卖验证） | 50并发同时下单同一商品（库存=10），验证最终仅生成10单 | 1号+3号 |
| S-04 | 安全 | SQL注入攻击 | 登录参数中注入 `' OR 1=1--`、`'; DROP TABLE--` 等，验证被拦截 | 1号 |
| S-05 | 安全 | Token篡改攻击 | 修改Token中间字段、伪造过期Token后访问 `/v1/user` | 1号 |
| S-06 | 安全 | 水平越权攻击 | 用户A的Token访问用户B的订单详情 `/v1/order/{B的订单ID}` | 1号+3号 |
| S-07 | 安全 | CMS未授权访问 | 普通用户Token访问 `/admin/group`、`/admin/menu` 等管理接口 | 2号 |
| S-08 | 安全 | 文件上传非法格式 | 上传 `.php`、`.jsp`、空文件等，验证被拦截 | 4号 |

### 产出目录

```
tests/system/
├── test_performance.py    # S-01, S-02, S-03
└── test_security.py       # S-04, S-05, S-06, S-07, S-08
```

### 报告产出

- §2.6 系统测试（上半部分）：性能测试报告（含TPS/延迟图表）+ 安全测试报告（含攻击向量和防御结果）

---

## 6. 4号 — 系统测试B（恢复 + 部署 + 稳定性）

### 测试范围

恢复测试3个场景 + 部署测试2个场景 + 稳定性/兼容性2个场景，共7个场景。

### 系统测试清单（7个场景）

| 编号 | 类型 | 场景描述 | 测试方法 | 目标模块 |
|------|------|---------|---------|---------|
| S-09 | 恢复 | 服务kill后重启验证 | `kill` Flask进程后重启，验证Token仍有效、数据完整、订单状态正确 | 全模块 |
| S-10 | 恢复 | 数据库断连恢复 | 手动断开MySQL连接后恢复，验证服务自动重连、接口恢复正常 | 全模块 |
| S-11 | 恢复 | 订单事务回滚验证 | 下单中途触发数据库异常，验证 `order` 表和 `order_product` 表无脏数据残留 | 1号+3号 |
| S-12 | 部署 | 环境变量切换验证 | 分别以 `ENV_MODE=dev:local`、`dev`、`prod` 启动，验证配置加载正确 | 4号 |
| S-13 | 部署 | 接口文档可用性 | 验证 Swagger `/apidocs/#/` 可访问、接口列表与代码一致 | 4号 |
| S-14 | 稳定性 | 持续30分钟请求 | 循环调用 `/v1/products` + `/v1/token`，每5分钟采样内存和错误率 | 全模块 |
| S-15 | 兼容 | 多角色访问验证 | 超级管理员/管理员/普通用户/未登录四种角色分别访问v1和CMS接口 | 全模块 |

### 产出目录

```
tests/system/
├── test_recovery.py       # S-09, S-10, S-11
├── test_deployment.py     # S-12, S-13
└── test_stability.py      # S-14, S-15
```

### 报告产出

- §2.6 系统测试（下半部分）：恢复测试报告 + 部署验证报告 + 稳定性监测报告

---

## 7. 一周时间安排

| | 周一 | 周二 | 周三 | 周四 | 周五 | 周六 | 周日 |
|------|------|------|------|------|------|------|------|
| **1号** | 写validator+token_auth单测 | 写utils+auth单测 | 画3张流程图 + 统计分支覆盖 | 整理§2.2+§2.3报告 | 撰写§4综合评价(初稿) | 汇总润色 | 终审 |
| **2号** | I-01鉴权链路 + I-02注册改密链路 | I-03下单支付链路 | I-04 RBAC + I-05文件+I-06身份 | 整理§2.4报告 | 撰写§3测试与总结 | 汇总润色 | 终审 |
| **3号** | S-01/02性能压测(写脚本+执行) | S-03并发下单 + S-04/05 SQL注入+Token | S-06/07/08越权+CMS+文件安全 | 整理性能+安全报告 | 协助1号写§1概述 | 汇总润色 | 终审 |
| **4号** | S-09/10服务重启+DB恢复 | S-11事务回滚 + S-12环境切换 | S-13 Swagger + S-14稳定性30min | S-15兼容性 + 整理报告 | 撰写§4综合评价(初稿) | 汇总润色 | 终审 |

---

## 8. 报告章节归属总览

| 章节 | 标题 | 负责人 | 状态 |
|------|------|--------|------|
| — | 封面、成员分工、成绩考核表 | 组长 | ⬜ |
| — | 题目、摘要、关键字 | 全组 | ⬜ |
| 第1章 | 测试概述 | **3号**（主笔）+ 全组提供素材 | ⬜ |
| §2.1 | 黑盒测试 | 从已完成接口用例中提取 | ✅ |
| §2.2 | 白盒测试 | **1号** | ❌ |
| §2.3 | 单元测试 | **1号** | ❌ |
| §2.4 | 集成测试 | **2号** | ❌ |
| §2.5 | 接口测试 | 已完成 | ✅ |
| §2.6 | 系统测试 | **3号**(性能+安全) + **4号**(恢复+部署+稳定) | ❌ |
| 第3章 | 测试与总结 | **2号**汇总 | ⬜ |
| 第4章 | 综合评价 | **1号** + **4号**合写 | ⬜ |

---

## 9. 代码目录总览

```
tests/
├── unit/                        # 1号：单元测试
│   ├── __init__.py
│   ├── test_validator.py        # isPositiveInteger / isNaturalNumber / isList
│   ├── test_token_auth.py       # generate_auth_token / decrypt_token
│   ├── test_utils.py            # as_namedtuple / get_request_args
│   └── test_auth.py             # get_ep_name / find_auth_module / is_in_auth_scope
│
├── integration/                 # 2号：集成测试
│   ├── __init__.py
│   ├── conftest.py
│   ├── test_auth_flow.py        # I-01 登录→Token→访问受限接口
│   ├── test_user_lifecycle.py   # I-02 注册→改密码, I-06 绑定→解绑
│   ├── test_order_flow.py       # I-03 下单→库存→快照→支付回调
│   ├── test_rbac_flow.py        # I-04 RBAC鉴权
│   └── test_file_flow.py        # I-05 文件上传→静态访问
│
├── system/                      # 3号+4号：系统测试
│   ├── test_performance.py      # 3号 S-01~03 性能压测
│   ├── test_security.py         # 3号 S-04~08 安全渗透
│   ├── test_recovery.py         # 4号 S-09~11 异常恢复
│   ├── test_deployment.py       # 4号 S-12~13 部署验证
│   └── test_stability.py        # 4号 S-14~15 稳定性+兼容性
│
├── api/                         # 已完成：接口测试
│   ├── 1号/                     # test_auth_cases.py / test_user_cases.py 等
│   ├── 2号/                     # test_group_cases.py / test_menu_cases.py 等
│   ├── 3号/                     # test_order_business_cases.py 等
│   └── 4号/                     # test_deploy_cases.py / test_file_cases.py 等
│
├── cases/                       # 已完成：接口测试YAML数据
│   ├── 1号/                     # auth_cases.yaml / user_cases.yaml 等
│   ├── 2号/                     # group_cases.yaml / menu_cases.yaml 等
│   ├── 3号/                     # order_business_cases.yaml 等
│   └── 4号/                     # deploy_cases.yaml / file_cases.yaml 等
│
├── common/                      # 公共工具
│   ├── api_client.py
│   ├── case_loader.py
│   ├── allure_helper.py
│   └── auth_seed.py
│
└── conftest.py                  # session级fixture (base_url/timeout等)
```

---

## 10. 分工合理性说明

```
        旧方案：横向（按模块）
        ┌──────┬──────┬──────┬──────┐
  1号   │ auth │ user │product│order │  全是接口测试
  2号   │group │ menu │config │ dict │  全是接口测试
  3号   │catalog│order│idempot│      │  全是接口测试
  4号   │deploy│ file│  log  │ swgr │  全是接口测试
        └──────┴──────┴──────┴──────┘
        → 每人都做同一层次，报告只有接口测试有内容

        新方案：纵向（按测试层次）
        ┌────────┬────────┬────────┬────────┐
  auth  │ 单元测试│ 集成测试│ 系统A  │ 系统B  │
  user  │ (1号)  │ (2号)  │ (3号)  │ (4号)  │
product │ 各模块  │ 各模块  │ 各模块  │ 各模块  │
 order  │ 都覆盖  │ 都覆盖  │ 都覆盖  │ 都覆盖  │
 group  │        │        │        │        │
  menu  │        │        │        │        │
  file  │        │        │        │        │
  ...   │        │        │        │        │
        └────────┴────────┴────────┴────────┘
        → 每人负责不同测试层次，报告六个测试类型全部有内容
```
