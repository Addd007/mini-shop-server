# 异常恢复测试用例

> 责任人：4号  
> 测试基线：`/apispec_1.json`  
> 覆盖范围：健康检查、错误处理、超大请求体、稳定性连续请求、SQL 注入防护、超长参数处理

## 健康检查

| 用例编号         | 接口方法 | 接口路径      | 测试点 | 用例标题       | 前置条件 | 请求数据 | 预期结果   | 优先级 |
| ------------ | ---- | --------- | --- | ---------- | ---- | ---- | ------ | --- |
| TC-RECOV-001 | GET  | `/health` | 异常恢复 | 健康检查端点可用   | 无    | 无    | 返回 200 | P0  |

## 错误处理

| 用例编号         | 接口方法 | 接口路径                              | 测试点 | 用例标题             | 前置条件 | 请求数据                       | 预期结果   | 优先级 |
| ------------ | ---- | --------------------------------- | --- | ---------------- | ---- | -------------------------- | ------ | --- |
| TC-RECOV-002 | GET  | `/api/nonexistent_endpoint_xyz`   | 异常恢复 | 请求不存在路由返回 404      | 无    | 无                          | 返回 404 | P1  |
| TC-RECOV-003 | POST | `/v1/token`                       | 异常恢复 | 无效 JSON 格式请求被正确处理  | 无    | `{invalid json!!!}`        | 返回 400 | P1  |
| TC-RECOV-004 | POST | `/v1/token`                       | 异常恢复 | 超大请求体处理不崩溃        | 无    | `account=super` `secret=123456` `type=100` + 超大附属字段 | 返回 200 | P2  |

## 稳定性

| 用例编号         | 接口方法 | 接口路径              | 测试点 | 用例标题          | 前置条件  | 请求数据        | 预期结果          | 优先级 |
| ------------ | ---- | ----------------- | --- | ------------- | ----- | ----------- | ------------- | --- |
| TC-RECOV-005 | GET  | `/cms/file/types` | 异常恢复 | 连续请求后服务仍稳定    | 已登录管理员 | 连续请求 50 次   | 50 次全部返回 200  | P2  |

## 安全防护

| 用例编号         | 接口方法 | 接口路径                                      | 测试点 | 用例标题                      | 前置条件  | 请求数据 | 预期结果   | 优先级 |
| ------------ | ---- | ----------------------------------------- | --- | ------------------------- | ----- | ---- | ------ | --- |
| TC-RECOV-006 | GET  | `/cms/file/name/'; DROP TABLE file; --`   | 异常恢复 | 特殊字符参数处理（SQL注入尝试）         | 已登录管理员 | 无    | 返回 200 | P1  |

## 边界条件

| 用例编号         | 接口方法 | 接口路径                      | 测试点 | 用例标题          | 前置条件  | 请求数据      | 预期结果   | 优先级 |
| ------------ | ---- | ------------------------- | --- | ------------- | ----- | --------- | ------ | --- |
| TC-RECOV-007 | GET  | `/cms/file/name/{超长字符串}`   | 异常恢复 | 超长参数处理不崩溃     | 已登录管理员 | `filename=<超长字符串>` | 返回 200 | P2  |

## 回归建议

- 核心冒烟：`TC-RECOV-001`、`TC-RECOV-002`
- 错误处理：`TC-RECOV-003`、`TC-RECOV-004`
- 稳定性：`TC-RECOV-005`
- 安全类：`TC-RECOV-006`、`TC-RECOV-007`
