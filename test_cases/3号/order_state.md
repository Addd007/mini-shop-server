# 订单状态流转完整测试用例

> **测试模块：订单状态管理与后台发货模块（Order State / CMS Delivery）**  
> 责任人：3号  
> 测试类型：状态迁移测试、接口测试、数据库一致性测试  
> 执行日期：2026-06-19

## 1. 公共测试数据

| 数据类型 | 测试数据 |
|---|---|
| 管理账号 | `super / 123456`，用户ID 1，携带有效Token |
| 测试订单 | 订单ID 1，订单号`B0X435186095427189` |
| 状态定义 | 1待支付、2已支付、3已发货、4已支付库存不足、5已处理库存不足 |
| 发货接口 | `PUT /cms/order/delivery?order_id={id}` |
| 不存在订单 | ID `999999` |

## 2. 完整测试用例

| 用例编号 | 用例标题 | 前置条件 | 测试数据/请求 | 执行步骤 | 预期结果 | 数据库检查 | 优先级 | 实际结果 | 判定/缺陷 |
|---|---|---|---|---|---|---|---|---|---|
| TC-M3-STATE-001 | 待支付订单不能发货 | 将订单1状态置为1；管理员已登录 | `PUT /cms/order/delivery?order_id=1` | 1.查询初始状态；2.发送发货请求；3.再次查询订单 | HTTP 403；`error_code=8002`；提示订单未支付 | `order_status`仍为1 | P0 | 返回403，状态未变 | 通过 |
| TC-M3-STATE-002 | 已支付订单可以发货 | 将订单1状态置为2 | 同上，`order_id=1` | 1.设置状态2；2.发货；3.查接口和数据库 | HTTP 200；`error_code=0`；订单由已支付变为已发货 | `order_status=3` | P0 | 返回403，错误认为未支付 | 失败；BUG-ORDER-STATE-001 |
| TC-M3-STATE-003 | 已发货订单不能重复发货 | 将订单1状态置为3 | 同上，`order_id=1` | 1.设置状态3；2.再次发货；3.检查状态 | HTTP 403；不得重复更新 | `order_status`仍为3 | P1 | 返回403，状态未变 | 通过 |
| TC-M3-STATE-004 | 不存在订单不能发货 | ID 999999不存在 | `PUT /cms/order/delivery?order_id=999999` | 携带管理员Token发送请求 | HTTP 404；返回订单不存在；不新增或修改订单 | 订单总数不变 | P1 | 返回404 | 通过 |
| TC-M3-STATE-005 | 非法订单ID被校验 | 管理员已登录 | `PUT /cms/order/delivery?order_id=0` | 发送ID为0的请求 | HTTP 400；提示ID必须为正整数 | 所有订单状态不变 | P1 | 返回400 | 通过 |
| TC-M3-STATE-006 | 非法流转后事务不修改状态 | 订单1状态为1 | 发货请求，`order_id=1` | 记录状态；执行必然失败的流转；重新查询 | 请求失败且无部分更新 | 状态仍为1，更新时间无异常变化 | P0 | 状态保持1 | 通过 |
| TC-M3-STATE-007 | 前后台订单状态一致 | 订单1状态为1；管理员登录 | `GET /v1/order/1`；`GET /cms/order/search?order_no=B0X435186095427189` | 分别查询前台详情、后台详情和数据库 | 三处`order_status`均为1，订单号和ID对应一致 | 数据库状态=1 | P1 | 三处状态一致 | 通过 |

## 3. 自动化对应关系

- 数据文件：`tests/cases/3号/order_state_cases.yaml`
- 自动化脚本：`tests/api/3号/test_order_state_cases.py`
- Allure结果：`reports/allure-results/member3-order-state`
- 执行结果：6通过，1失败
