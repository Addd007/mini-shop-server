# 性能抽测测试用例

> 责任人：4号  
> 测试基线：`/apispec_1.json`  
> 覆盖范围：单接口响应时间、并发登录、并发查询

## 单接口响应时间

| 用例编号       | 接口方法 | 接口路径              | 测试点 | 用例标题               | 前置条件  | 请求数据                                           | 预期结果              | 优先级 |
| ---------- | ---- | ----------------- | --- | ------------------ | ----- | ---------------------------------------------- | ----------------- | --- |
| TC-PERF-001 | GET  | `/cms/file/types` | 性能  | 简单接口响应时间（文件类型查询）    | 已登录管理员 | 无                                              | 返回 200，响应时间 < 500ms | P2  |
| TC-PERF-002 | POST | `/v1/token`       | 性能  | 登录接口响应时间           | 无     | `account=super` `secret=123456` `type=100`      | 返回 200，响应时间 < 1000ms | P2  |
| TC-PERF-003 | GET  | `/apispec_1.json` | 性能  | Swagger JSON 获取响应时间 | 无     | 无                                              | 返回 200，响应时间 < 3000ms | P2  |

## 并发性能测试

| 用例编号       | 接口方法 | 接口路径              | 测试点  | 用例标题         | 前置条件  | 请求数据                                           | 预期结果                 | 优先级 |
| ---------- | ---- | ----------------- | ---- | ------------ | ----- | ---------------------------------------------- | -------------------- | --- |
| TC-PERF-004 | POST | `/v1/token`       | 并发性能 | 并发登录测试（10并发）  | 无     | `account=super` `secret=123456` `type=100`      | 成功率 > 90%，平均响应 < 2000ms | P2  |
| TC-PERF-005 | GET  | `/cms/file/types` | 并发性能 | 并发查询测试（20并发）  | 已登录管理员 | 无                                              | 成功率 > 95%，平均响应 < 1000ms | P2  |

## 回归建议

- 响应时间基准：`TC-PERF-001`、`TC-PERF-002`
- 并发稳定性：`TC-PERF-004`、`TC-PERF-005`
- Swagger 大文档：`TC-PERF-003`
