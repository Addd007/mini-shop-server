# mini-shop-server 测试计划

> 文档版本：v1.0
> 
> 编写日期：2026-06-09
> 
> 适用项目：mini-shop-server（Flask 微信小程序商城后端）
> 
> 文档状态：草案 / 待评审

---

## 1. 文档目的

本文档用于规范 `mini-shop-server` 项目的测试活动，明确测试目标、范围、策略、资源、进度、准入准出标准、风险控制及交付物要求，确保系统在功能正确性、接口稳定性、安全性、兼容性、性能和可运维性等方面达到发布标准。

---

## 2. 项目背景

`mini-shop-server` 是一个基于 Flask 构建的微信小程序商城后端系统，提供用户、权限、菜单、字典、参数、通知、日志、接口文档、文件上传、文章等核心能力，并通过 Swagger 风格接口文档向前端与测试团队开放接口说明。

由于本项目同时涉及：

- 面向小程序端的业务接口
- 面向管理后台的管理接口
- 文件上传与静态资源访问
- 数据库读写与事务一致性
- Token 鉴权与权限控制
- 线上/线下环境配置差异

因此测试需要采用“功能测试 + 接口测试 + 回归测试 + 非功能测试”的组合模式，确保业务发布质量。

---

## 3. 测试目标

### 3.1 总体目标

验证系统在目标运行环境下能够稳定、正确、安全地完成预期业务功能，满足上线标准与运维要求。

### 3.2 具体目标

1. 验证核心业务接口的正确性与完整性。
2. 验证鉴权、权限分级、数据隔离等安全控制有效。
3. 验证上传、下载、静态资源访问等高频能力稳定可用。
4. 验证错误处理、日志、异常返回、错误码统一规范。
5. 验证不同环境配置切换正常，避免本地与线上配置串用。
6. 验证在基础并发场景下系统具备可接受的响应时间与稳定性。
7. 验证发布前回归闭环，降低版本回退风险。

---

## 4. 测试范围

### 4.1 测试范围内模块

| 模块 | 测试内容 | 说明 |
|---|---|---|
| 用户管理 | 注册、登录、信息查询、状态校验 | 验证账户生命周期 |
| Token 鉴权 | Token 获取、校验、失效、刷新策略（如适用） | 保障认证有效性 |
| 权限组/角色管理 | 角色创建、授权、菜单权限分配 | 验证 RBAC 能力 |
| 菜单管理 | 菜单查询、多级菜单、动态路由数据 | 验证前端配置支持 |
| 字典管理 | 字典查询、维护、分类正确性 | 验证系统基础配置 |
| 参数管理 | 系统参数查询与修改 | 验证配置生效 |
| 通知管理 | 公告/通知的发布、查询、状态 | 验证信息触达能力 |
| 日志管理 | 操作日志、登录日志 | 验证审计追踪能力 |
| 商品/分类/主题/轮播 | 基础业务数据查询 | 验证商城展示数据 |
| 订单管理 | 订单创建、查询、状态流转 | 验证交易链路 |
| 支付相关 | 支付请求、回调、异常场景 | 验证支付链路与幂等性 |
| 文件管理与上传 | 上传、下载、目录/文件管理 | 验证资源处理能力 |
| 接口文档 | Swagger 文档访问与生成正确性 | 验证文档可用性 |
| 错误码与异常处理 | 错误码返回、异常拦截 | 验证统一规范 |
| 部署与配置 | uv、Gunicorn、Nginx、环境变量 | 验证部署链路可用 |

### 4.2 测试范围外内容

以下内容不纳入本次测试范围，除非在迭代计划中单独说明：

- 第三方平台服务本身的可用性保障（如微信平台、支付平台外部故障）
- 非本项目代码库内的前端页面视觉设计
- 历史遗留不可复现问题的深度逆向分析
- 生产环境数据修复、人工补单流程的最终执行

---

## 5. 测试策略

### 5.1 测试层级

1. **单元测试**
   - 面向工具函数、业务函数、数据校验逻辑、异常处理逻辑
   - 覆盖核心分支和边界条件

2. **接口测试**
   - 面向 API 接口进行请求/响应验证
   - 覆盖参数合法性、鉴权、业务流、幂等、异常码

3. **集成测试**
   - 验证 API、数据库、缓存、文件系统、第三方依赖之间的交互
   - 重点关注事务一致性和接口串联流程

4. **回归测试**
   - 对已修复缺陷和关键路径进行重复验证
   - 每次版本冻结前必须执行

5. **非功能测试**
   - 性能、并发、稳定性、安全、兼容性、可维护性

### 5.2 测试方法

- **黑盒测试**：以接口输入输出为主，验证业务功能是否符合需求。
- **边界值测试**：验证分页、字符串长度、空值、非法值等边界情况。
- **异常注入测试**：模拟数据库异常、权限不足、参数缺失、文件上传失败等情况。
- **接口串联测试**：模拟完整业务链路，例如登录 → 获取商品 → 下单 → 支付。
- **配置切换测试**：验证 `dev:local`、`dev`、`prod` 等环境变量切换的准确性。

### 5.3 测试执行原则

- 关键主流程优先
- 高风险模块优先
- 缺陷高发模块优先
- 先冒烟，后系统，最后回归
- 所有缺陷必须闭环跟踪至关闭或延期确认

---

## 6. 测试环境

### 6.1 环境说明

| 环境 | 用途 | 说明 |
|---|---|---|
| 本地开发环境 | 开发自测、接口联调 | 通过 `uv run` 启动 |
| 测试环境 | 正式测试执行 | 与生产隔离，数据可回收 |
| 预生产环境 | 发布前验证 | 配置尽量贴近生产 |
| 生产环境 | 上线后巡检 | 仅执行只读验证或灰度验证 |

### 6.2 环境依赖

- Python 3.8+
- uv 包管理工具
- MySQL
- Flask 2.0.3 及相关依赖
- Gunicorn
- Nginx（部署场景）
- Swagger / Flasgger 接口文档

### 6.3 测试数据准备

- 基础管理员账号
- 普通用户账号
- 角色账号（管理员、运营、只读、访客等）
- 商品、分类、轮播图、文章、字典、参数等基础数据
- 文件上传样本（图片、文档、异常格式文件）
- 支付/回调模拟数据（如适用）

---

## 7. 测试范围详细拆解

### 7.1 用户与认证

#### 测试点
- 正常登录返回 Token
- 错误凭证登录失败
- Token 缺失/过期/伪造处理
- 登录态与用户状态一致性
- 重复登录与会话覆盖策略（如有）

#### 验证重点
- 鉴权逻辑是否统一
- 错误码是否标准化
- 是否存在越权访问

### 7.2 权限与菜单

#### 测试点
- 角色新增/编辑/删除
- 角色授权是否生效
- 菜单树结构是否正确返回
- 不同权限用户看到的数据是否一致
- 越权访问是否被拦截

#### 验证重点
- RBAC 是否正确实施
- 前后端权限数据是否一致

### 7.3 参数、字典与通知

#### 测试点
- 字典项维护与查询
- 参数修改后是否生效
- 通知发布、查询、状态变更
- 空值与重复值处理

#### 验证重点
- 数据一致性
- 配置变更可追踪性

### 7.4 商品、分类、主题、轮播

#### 测试点
- 列表查询
- 分类层级
- 主题推荐数据
- 轮播图展示顺序与状态

#### 验证重点
- 前台展示数据正确性
- 排序与筛选逻辑

### 7.5 订单与支付

#### 测试点
- 下单成功路径
- 库存不足/参数不合法路径
- 订单状态流转
- 支付请求与回调
- 幂等性验证
- 重复提交处理

#### 验证重点
- 金额、状态、库存、事务一致性
- 回调可信性
- 防重复支付、防重复订单

### 7.6 文件上传与下载

#### 测试点
- 图片、文档上传
- 非法格式/超大文件/空文件上传
- 静态资源访问
- 路径权限与目录隔离
- 下载链接可用性

#### 验证重点
- 文件安全性
- 上传失败后的异常回滚
- 资源访问路径正确性

### 7.7 日志与审计

#### 测试点
- 登录日志记录
- 操作日志记录
- 错误日志记录
- 日志字段完整性

#### 验证重点
- 日志可追踪
- 审计信息完整

### 7.8 部署与配置

#### 测试点
- `uv sync` 是否可顺利安装依赖
- `uv run python server.py run` 启动成功
- Gunicorn 启动与端口绑定正常
- Nginx 反向代理与静态资源转发正常
- `ENV_MODE` 切换逻辑正确

#### 验证重点
- 启动流程可重复
- 配置变更不影响核心功能

---

## 8. 测试类型与覆盖要求

### 8.1 冒烟测试

用于版本初验，至少覆盖以下场景：
- 服务启动成功
- 接口文档可访问
- 登录接口可用
- 核心业务查询接口可用
- 文件上传接口可用
- 数据库连通正常

### 8.2 功能测试

覆盖所有需求点，要求：
- 正向流程覆盖率 100%
- 关键异常流程覆盖率 100%
- 重点模块边界值覆盖率不低于 90%

### 8.3 回归测试

每次修复缺陷后，必须执行：
- 缺陷相关用例
- 影响链路用例
- 关键业务主流程用例

### 8.4 安全测试

至少验证：
- 未登录访问限制
- Token 伪造
- 权限越界访问
- 常见非法输入
- 文件上传安全性

### 8.5 性能测试

建议覆盖：
- 核心查询接口响应时间
- 登录接口并发能力
- 列表分页接口压力
- 上传接口稳定性
- 订单相关高频场景

### 8.6 兼容性测试

建议覆盖：
- 不同浏览器访问接口文档
- 不同环境变量配置
- Linux 部署环境一致性

---

## 9. 测试用例设计原则

### 9.1 用例设计维度

- 功能维度
- 异常维度
- 边界维度
- 权限维度
- 数据维度
- 兼容维度
- 性能维度

### 9.2 用例命名规范

建议统一采用以下格式：

`模块_接口_场景_预期结果`

示例：
- `登录_获取Token_有效账号_返回200及Token`
- `文件上传_上传图片_非法格式_返回错误码`
- `订单_创建订单_库存不足_返回失败`

### 9.3 用例分级

| 级别 | 定义 | 执行要求 |
|---|---|---|
| P0 | 核心主流程，阻断上线 | 每次发布必测 |
| P1 | 重要功能，影响大部分用户 | 每次回归必测 |
| P2 | 常规功能，业务范围较小 | 按版本执行 |
| P3 | 低频功能或展示性内容 | 按需执行 |

---

## 10. 准入标准

测试开始前，需满足以下条件：

1. 需求说明已确认并冻结。
2. 接口文档已更新至最新版本。
3. 测试环境已部署完成，数据库已准备好。
4. 主要依赖服务可用。
5. 开发自测通过并提交可测试版本。
6. 核心接口无阻断性已知缺陷。
7. 测试数据与账号准备完毕。

---

## 11. 准出标准

版本可建议进入发布流程需满足：

1. P0 缺陷为 0。
2. P1 缺陷为 0，或已获得业务方批准延期。
3. 核心功能测试通过率 100%。
4. 回归测试通过率达到 100%。
5. 无阻断发布的安全问题。
6. 性能测试结果满足最低可接受标准。
7. 测试报告、缺陷清单、回归记录已归档。

---

## 12. 缺陷管理规范

### 12.1 缺陷等级定义

| 等级 | 定义 | 示例 |
|---|---|---|
| S1 致命 | 系统不可用、数据严重损坏、无法发布 | 服务启动失败、核心交易链路断裂 |
| S2 严重 | 核心功能不可用或存在严重越权风险 | 登录失败、支付错误、权限失控 |
| S3 一般 | 局部功能异常或存在明显体验问题 | 某查询筛选失效、列表排序错误 |
| S4 轻微 | 不影响主流程的展示/文案问题 | 文案错别字、样式偏差 |

### 12.2 缺陷处理流程

1. 发现缺陷并提交工单。
2. 测试人员补充复现步骤、截图、日志、请求响应。
3. 开发确认并修复。
4. 测试回归验证。
5. 关闭缺陷或重新打开。

### 12.3 缺陷记录要求

缺陷记录应至少包含：
- 标题
- 模块
- 严重级别
- 环境信息
- 复现步骤
- 预期结果
- 实际结果
- 影响范围
- 附件证据
- 修复版本
- 回归结果

---

## 13. 测试进度安排

> 以下为建议计划，可根据迭代节奏调整。

| 阶段 | 时间 | 主要任务 | 输出物 |
|---|---|---|---|
| 需求评审 | D1 | 梳理需求、确认范围 | 评审记录 |
| 测试设计 | D1-D2 | 编写测试点、测试用例 | 用例清单 |
| 环境准备 | D2 | 部署测试环境、准备数据 | 环境检查单 |
| 冒烟测试 | D3 | 验证基础可用性 | 冒烟结果 |
| 功能测试 | D3-D5 | 执行主功能与异常场景 | 缺陷记录 |
| 缺陷修复验证 | D4-D6 | 回归缺陷并复测 | 回归记录 |
| 非功能测试 | D5-D6 | 性能/安全/兼容测试 | 专项报告 |
| 测试总结 | D7 | 汇总结果并评审 | 测试报告 |

---

## 14. 资源与角色分工

### 14.1 角色分工

| 角色 | 职责 |
|---|---|
| 测试负责人 | 统筹计划、风险控制、结果评审 |
| 测试工程师 | 用例设计、执行测试、提交缺陷、回归验证 |
| 开发工程师 | 修复缺陷、提供技术支持、参与评审 |
| 产品/业务方 | 确认需求、验收结果、评估延期缺陷 |
| 运维/部署支持 | 提供环境、发布支持、监控巡检 |

### 14.2 资源需求

- 测试环境 1 套
- 数据库实例 1 套
- 测试账号若干
- 接口调试工具（如 Postman、Apifox 等）
- 缺陷管理系统或工单系统
- 日志查看与抓包工具

---

## 15. 风险识别与应对

| 风险 | 影响 | 概率 | 应对策略 |
|---|---|---|---|
| 需求频繁变更 | 用例失效、返工 | 高 | 需求冻结后再测，变更需重新评审 |
| 测试环境不稳定 | 阻碍执行 | 中 | 环境检查与快照恢复 |
| 测试数据污染 | 结果不可靠 | 中 | 数据隔离、定期重置 |
| 第三方服务不可用 | 主流程阻断 | 中 | 模拟数据与降级方案 |
| 接口文档未同步 | 漏测或误测 | 中 | 文档版本管理与同步校验 |
| 缺陷修复引入回归 | 质量波动 | 中 | 关键链路回归与冒烟门禁 |

---

## 16. 退出与发布建议

在满足准出标准后，测试团队应输出如下结论之一：

1. **建议发布**：无阻断缺陷，核心功能达标。
2. **有条件发布**：存在少量低优先级缺陷，但已形成风险接受结论。
3. **建议延期发布**：存在阻断性缺陷或关键风险未消除。

---

## 17. 交付物清单

测试阶段应输出以下文档或记录：

- 测试计划
- 测试用例清单
- 冒烟测试记录
- 功能测试记录
- 缺陷清单
- 回归测试记录
- 性能/安全专项测试记录
- 测试总结报告
- 发布建议结论

---

## 18. 附录

### 18.1 建议检查项

- [ ] 服务启动成功
- [ ] 接口文档正常访问
- [ ] 数据库连接正常
- [ ] 登录鉴权正常
- [ ] 核心 CRUD 接口正常
- [ ] 文件上传下载正常
- [ ] 权限控制正常
- [ ] 错误码返回正常
- [ ] 日志记录正常
- [ ] 回归用例全部通过

### 18.2 版本记录

| 版本 | 日期 | 编写人 | 说明 |
|---|---|---|---|
| v1.0 | 2026-06-09 | AI Assistant | 首次创建，企业级测试计划模板 |

---

## 19. 结论

本测试计划面向 `mini-shop-server` 的后端服务特点，覆盖了业务功能、接口鉴权、数据一致性、文件处理、部署配置及非功能维度，适合作为项目迭代、联调验收和上线前评审的标准测试依据。后续可在本计划基础上进一步细化为具体测试用例表、测试执行表与缺陷跟踪表。
