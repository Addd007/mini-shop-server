# mini-shop-server 测试启动流程说明

> 文档版本：v1.0  
> 编写日期：2026-06-09  
> 适用范围：mini-shop-server 测试环境启动、接口联调、自动化测试执行前准备  
> 文档状态：草案 / 可执行

---

## 1. 文档目的

本文档用于说明在 `mini-shop-server` 项目开发稳定、接口不再频繁变动的前提下，如何快速搭建并启动测试环境，确保测试人员可以直接进入接口测试、自动化测试和回归测试阶段。

如果项目代码已基本稳定，当前阶段**适合直接启动项目作为测试环境**，并在此基础上进行测试数据和配置的隔离管理。

---

## 2. 环境使用原则

### 2.1 当前推荐方式

如果项目开发已基本不会再改动，建议采用以下方式：

- 直接启动项目作为测试环境
- 使用独立的测试数据库
- 使用测试专用配置文件
- 使用测试账号和测试文件
- 避免与开发环境共用数据

### 2.2 适用场景

以下场景适合直接启动项目作为测试环境：

- 接口功能测试
- 冒烟测试
- 自动化测试框架验证
- 回归测试
- 联调测试
- 版本验收前检查

### 2.3 不建议直接使用的场景

以下场景建议额外搭建独立测试机或测试服务器：

- 多人并行高频测试
- 自动化定时执行
- 预生产验证
- 接近上线前的稳定回归
- 需要和开发环境完全隔离的正式测试

---

## 3. 启动前准备

### 3.1 基础依赖

请先确认以下基础依赖已安装：

- Python 3.8+
- `uv`
- MySQL
- Git

### 3.2 项目代码准备

进入项目根目录后确认代码完整：

- `pyproject.toml`
- `server.py`
- `config/`
- `app/`
- `tests/`

### 3.3 测试数据准备

建议提前准备以下测试数据：

- 测试管理员账号
- 普通测试用户账号
- 停用账号（如有）
- 商品数据
- 分类数据
- 轮播图数据
- 文件上传样本
- 测试订单数据

---

## 4. 启动流程

### 步骤 1：进入项目目录

确认当前目录为项目根目录。

### 步骤 2：安装依赖

使用 `uv` 安装项目依赖。

```bash
uv sync
```

如果需要补充测试依赖，也可以执行：

```bash
uv sync --extra dev
```

### 步骤 3：确认测试配置

检查测试配置文件：

- `config/test.yaml`

重点确认：

- `base_url`
- 测试账号
- 超时时间
- 上传文件路径

### 步骤 4：确认数据库连接

确认项目连接的是测试数据库，而不是开发库或生产库。

重点检查：

- 数据库地址
- 数据库名
- 用户名
- 密码
- 字符集

### 步骤 5：启动项目

推荐启动方式如下：

```bash
uv run python server.py run
```

如果需要指定端口：

```bash
uv run python server.py run -p 8080
```

如果需要对外可访问：

```bash
uv run python server.py run -h 0.0.0.0 -p 8080
```

### 步骤 6：验证服务是否成功启动

启动后检查：

- 服务日志无报错
- 端口正常监听
- 接口文档可访问
- 数据库连接正常

---

## 5. 启动后验证项

### 5.1 基础检查

启动后先验证以下内容：

1. 接口文档可访问
   - 访问 `/apidocs/#/`
2. 登录接口可调用
   - `POST /v1/token`
3. 用户信息接口可调用
   - `GET /v1/user/me`
4. 商品列表接口可调用
   - `GET /v1/products`
5. 文件上传接口可调用
   - `POST /v2/files/upload`

### 5.2 冒烟检查建议

建议启动成功后先做冒烟测试，至少覆盖：

- 服务启动
- 登录成功
- Token 可用
- 商品列表可查
- 文件上传可用

---

## 6. 测试环境配置建议

### 6.1 环境变量

建议使用环境变量区分环境，例如：

- `ENV_MODE=dev:local`
- `ENV_MODE=dev`
- `ENV_MODE=prod`

对于测试环境，建议单独使用测试配置。

### 6.2 配置文件

建议明确区分以下文件：

- `config/test.yaml`：测试环境配置
- `config/dev.yaml`：开发环境配置
- `config/prod.yaml`：生产环境配置

### 6.3 上传目录

文件上传和静态资源目录建议与开发目录隔离，避免互相覆盖。

---

## 7. 自动化测试启动方式

如果你准备执行 YAML 驱动自动化测试，可以按以下方式启动：

### 7.1 先确认测试环境已启动

确保项目服务已经运行，并且 `base_url` 正确。

### 7.2 再执行自动化测试

执行 pytest 相关测试：

```bash
uv run pytest
```

只跑认证相关测试：

```bash
uv run pytest -m auth
```

只跑冒烟测试：

```bash
uv run pytest -m smoke
```

### 7.3 查看测试结果

建议在测试执行后检查：

- 测试报告
- 日志输出
- 失败用例
- 请求与响应内容

---

## 8. 常见问题排查

### 8.1 服务启动失败

检查项：

- Python 版本是否正确
- 依赖是否安装完整
- 数据库连接是否正常
- 配置文件是否正确

### 8.2 接口文档打不开

检查项：

- 服务是否真正启动
- 端口是否正确
- Nginx 或防火墙是否拦截

### 8.3 登录失败

检查项：

- 测试账号是否正确
- 密码是否正确
- 账号是否被停用
- 请求参数格式是否正确

### 8.4 文件上传失败

检查项：

- 文件类型是否允许
- 文件大小是否超限
- 路径是否正确
- 上传目录权限是否正常

---

## 9. 推荐启动检查清单

- [ ] 项目依赖已安装
- [ ] 测试数据库已准备
- [ ] 测试配置已切换
- [ ] 测试账号已准备
- [ ] 服务已启动成功
- [ ] 接口文档可访问
- [ ] 登录接口可用
- [ ] Token 可正常获取
- [ ] 商品接口可查询
- [ ] 文件上传接口可用
- [ ] 自动化测试可执行

---

## 10. 结论

如果当前项目开发已基本稳定、不再频繁修改，那么**直接启动这个项目作为测试环境是完全可行的**。对于你们当前阶段，建议先使用本地或测试机直接启动，配合测试数据库和测试配置文件即可开展测试；等进入正式回归或多人协作阶段，再进一步升级为独立测试服务器。
