# 部署配置验证测试用例

> 责任人：4号  
> 测试基线：`/apispec_1.json`  
> 覆盖范围：服务根路径、CORS 跨域、Content-Type、Flask-Admin 后台、OPTIONS 预检、默认路由重定向

## 服务根路径

| 用例编号         | 接口方法 | 接口路径 | 测试点 | 用例标题        | 前置条件 | 请求数据 | 预期结果   | 优先级 |
| ------------ | ---- | ---- | --- | ----------- | ---- | ---- | ------ | --- |
| TC-DEPLOY-001 | GET  | `/`  | 部署  | 服务根路径可访问    | 无    | 无    | 返回 200 | P0  |

## CORS 跨域验证

| 用例编号         | 接口方法 | 接口路径       | 测试点 | 用例标题          | 前置条件 | 请求数据 | 预期结果           | 优先级 |
| ------------ | ---- | ---------- | --- | ------------- | ---- | ---- | -------------- | --- |
| TC-DEPLOY-002 | GET  | `/v1/token` | 部署  | CORS 跨域头正确设置  | 无    | 无    | 响应头包含 `Access-Control-*` | P1  |

## Content-Type 验证

| 用例编号         | 接口方法 | 接口路径              | 测试点 | 用例标题                  | 前置条件  | 请求数据 | 预期结果                      | 优先级 |
| ------------ | ---- | ----------------- | --- | --------------------- | ----- | ---- | ------------------------- | --- |
| TC-DEPLOY-003 | GET  | `/cms/file/types` | 部署  | JSON 响应 Content-Type 正确 | 已登录管理员 | 无    | 返回 200，`Content-Type: application/json` | P1  |

## Flask-Admin 后台管理

| 用例编号         | 接口方法 | 接口路径      | 测试点 | 用例标题                    | 前置条件 | 请求数据 | 预期结果   | 优先级 |
| ------------ | ---- | --------- | --- | ----------------------- | ---- | ---- | ------ | --- |
| TC-DEPLOY-004 | GET  | `/admin/` | 部署  | Flask-Admin 后台管理页面可访问    | 无    | 无    | 返回 200 | P1  |

## OPTIONS 预检请求

| 用例编号         | 接口方法   | 接口路径       | 测试点 | 用例标题             | 前置条件 | 请求数据 | 预期结果            | 优先级 |
| ------------ | ------ | ---------- | --- | ---------------- | ---- | ---- | --------------- | --- |
| TC-DEPLOY-005 | OPTIONS | `/v1/token` | 部署  | 验证 OPTIONS 预检请求处理 | 无    | 无    | 返回 200，含 CORS 响应头 | P2  |

## 默认路由

| 用例编号         | 接口方法 | 接口路径 | 测试点 | 用例标题        | 前置条件 | 请求数据 | 预期结果         | 优先级 |
| ------------ | ---- | ---- | --- | ----------- | ---- | ---- | ------------ | --- |
| TC-DEPLOY-006 | GET  | `/`  | 部署  | 默认路由重定向正常   | 无    | 无    | 返回 200，允许跟随重定向 | P2  |

## 回归建议

- 核心冒烟：`TC-DEPLOY-001`、`TC-DEPLOY-004`
- CORS/Content-Type：`TC-DEPLOY-002`、`TC-DEPLOY-003`
- 预检与路由：`TC-DEPLOY-005`、`TC-DEPLOY-006`
