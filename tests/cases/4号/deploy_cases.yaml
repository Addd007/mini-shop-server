# 部署配置验证 — 4号专项

# ======================== 服务根路径 ========================

- id: TC-DEPLOY-001
  summary: 服务根路径可访问
  tag: deploy
  priority: P0
  method: GET
  path: /
  no_auth: true
  expected:
    status_code: 200

# ======================== CORS 跨域验证 ========================

- id: TC-DEPLOY-002
  summary: CORS 跨域头正确设置
  tag: deploy
  priority: P1
  method: GET
  path: /v1/token
  no_auth: true
  expected:
    status_code: 200
  check_cors: true

# ======================== Content-Type 验证 ========================

- id: TC-DEPLOY-003
  summary: JSON 响应 Content-Type 正确
  tag: deploy
  priority: P1
  method: GET
  path: /cms/file/types
  expected:
    status_code: 200
  check_content_type_json: true

# ======================== Flask-Admin 后台管理 ========================

- id: TC-DEPLOY-004
  summary: Flask-Admin 后台管理页面可访问
  tag: deploy
  priority: P1
  method: GET
  path: /admin/
  no_auth: true
  expected:
    status_code: 200

# ======================== OPTIONS 预检请求 ========================

- id: TC-DEPLOY-005
  summary: 验证 OPTIONS 预检请求处理
  tag: deploy
  priority: P2
  method: OPTIONS
  path: /v1/token
  no_auth: true
  check_cors_preflight: true
  expected:
    status_code: 200

# ======================== 默认路由 ========================

- id: TC-DEPLOY-006
  summary: 默认路由重定向正常
  tag: deploy
  priority: P2
  method: GET
  path: /
  no_auth: true
  allow_redirects: true
  expected:
    status_code: 200
