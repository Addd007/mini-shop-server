# 环境变量切换测试用例

> 责任人：4号  
> 测试基线：`/apispec_1.json`  
> 覆盖范围：DEBUG 模式检查、服务端 URL 配置、应用版本信息、数据库连接验证、敏感信息泄露检查

## DEBUG 模式检查

| 用例编号      | 接口方法 | 接口路径              | 测试点 | 用例标题       | 前置条件  | 请求数据 | 预期结果               | 优先级 |
| --------- | ---- | ----------------- | --- | ---------- | ----- | ---- | ------------------ | --- |
| TC-ENV-001 | GET  | `/cms/file/types` | 环境  | DEBUG 模式确认  | 已登录管理员 | 无    | 返回 200，确认 DEBUG 模式状态 | P1  |

## 服务端 URL 配置

| 用例编号      | 接口方法 | 接口路径              | 测试点 | 用例标题          | 前置条件 | 请求数据 | 预期结果               | 优先级 |
| --------- | ---- | ----------------- | --- | ------------- | ---- | ---- | ------------------ | --- |
| TC-ENV-002 | GET  | `/apispec_1.json` | 环境  | 验证服务端 URL 配置   | 无    | 无    | 返回 200，JSON 含 `host` 字段 | P2  |

## 应用版本信息

| 用例编号      | 接口方法 | 接口路径      | 测试点 | 用例标题            | 前置条件 | 请求数据 | 预期结果   | 优先级 |
| --------- | ---- | --------- | --- | --------------- | ---- | ---- | ------ | --- |
| TC-ENV-003 | GET  | `/health` | 环境  | 验证 Flask 应用版本信息 | 无    | 无    | 返回 200 | P2  |

## 数据库连接验证

| 用例编号      | 接口方法 | 接口路径              | 测试点 | 用例标题                 | 前置条件  | 请求数据 | 预期结果        | 优先级 |
| --------- | ---- | ----------------- | --- | -------------------- | ----- | ---- | ----------- | --- |
| TC-ENV-004 | GET  | `/cms/file/types` | 环境  | 验证数据库连接正常（通过间接查询）   | 已登录管理员 | 无    | 返回 200，数据正常返回 | P1  |

## 敏感信息泄露检查

| 用例编号      | 接口方法 | 接口路径      | 测试点 | 用例标题             | 前置条件 | 请求数据 | 预期结果               | 优先级 |
| --------- | ---- | --------- | --- | ---------------- | ---- | ---- | ------------------ | --- |
| TC-ENV-005 | GET  | `/health` | 环境  | 验证响应头无敏感信息泄露     | 无    | 无    | 返回 200，响应头不含 Server 等泄露字段 | P1  |

## 回归建议

- 核心检查：`TC-ENV-001`、`TC-ENV-004`
- 配置验证：`TC-ENV-002`、`TC-ENV-003`
- 安全类：`TC-ENV-005`
